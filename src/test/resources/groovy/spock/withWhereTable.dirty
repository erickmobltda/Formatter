class MathSpec extends Specification {

    def "should add two numbers"() {
        expect:
        a + b == result

        where:
        a | b | result
        1 | 2 | 3
        4 | 5 | 9
        0 | 0 | 0
    }
}
